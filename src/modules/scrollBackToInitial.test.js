import { scrollBackToInitial } from "./scrollBackToInitial";

describe("scrollBackToInitial.js", () => {
    describe("scrollBackToInitial()", () => {
        test.todo('articlesがかわるとスクロール位置が戻る')
        
    });
    describe("scrollBackToInitial()", () => {
        test.todo('articlesがかわるとスクロール位置が戻る')
    });
    
});